const Users = require('./User')
const Chats = require('./Chat')
const Friendships = require('./Friendship')
const Messages = require('./Message')

module.exports = {
  Users, Chats, Friendships, Messages
}
